import styled from 'styled-components';

import { Row } from '../commons/commonStyles';

const SavedPaletteContainer = styled(Row)`
  padding: 8px;
  flex-direction: column;
`;

export default SavedPaletteContainer;
